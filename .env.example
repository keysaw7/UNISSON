# Copier en .env pour activer la persistance Postgres.
# Sans DATABASE_URL, l'API démarre avec les adapters en mémoire (dev/CI).
DATABASE_URL=postgres://unisson:unisson@localhost:5432/unisson
PORT=3000
